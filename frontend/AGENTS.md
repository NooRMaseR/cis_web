<!-- BEGIN:nextjs-agent-rules -->
# how to install

install a package for frontend using `bun add <package names>`
install a package for backend using `uv add <package names>`

# how to run

you can run the project in dev mode using `bun run dev`
